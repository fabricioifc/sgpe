<%- model = Plan %>

<%= render 'form', plan: @plan, url: offer_offer_discipline_plans_path %>
<!-- <= link_to t('helpers.links.back'), offer_offer_discipline_plans_path(offer_discipline_id: params[:offer_discipline_id]) %> -->
