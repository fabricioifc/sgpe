<!-- <= bootstrap_form_for(perfil_role, local:true, html: { class: "", role: "form" }) do |f| %>
  <= error_messages_for(perfil_role) %>


    <= f.select :perfil_id, Perfil.where(idativo:true).collect { |c| [ c.name, c.id] },
      { prompt:true, id: :perfil_role_perfil_id }, { class: 'select2', autofocus:true } %>
    <= f.select :role_id, Role.order(:name).collect { |c| [ c.name, c.id] },
      { prompt:true, id: :perfil_role_role_id }, { class: 'select2' } %>

    <= f.submit class: "btn btn-primary" %>
< end %> -->
