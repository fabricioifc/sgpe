<%- model = Grid %>

<div class="panel panel-default">
  <div class="panel-heading clearfix">
    <h3 class="panel-title pull-left">
      <%= link_to new_grid_path do %>
        <i class="fa fa-plus" aria-hidden="true"></i>
        <%= t '.title', :default => model.model_name.human.pluralize %>
      <% end %>
    </h3>
  </div>
  <div class="panel-body">
    <div class="alert alert-info">
      Caso possua um arquivo xls com informações sobre as grades/PPCs, existe a possibilidade de realizar a sua importação.
      <%= link_to "Desejo importar um arquivo de grades", importar_grids_path, class: "btn btn-primary" %>
    </div>
    <div class="table-responsive">
      <%= content_tag :table, id: "grids_datatable", class: 'table table-striped table-bordered', role: :datatable, data: { url: grids_path(format: :json)} do %>
        <thead>
          <tr>
            <th><%= model.human_attribute_name(:year) %></th>
            <th><%= model.human_attribute_name(:course_id) %></th>
            <th><%= model.human_attribute_name(:active) %></th>
            <th></th>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tbody></tbody>
      <% end %>
    </div>
  </div>
  <div class="panel-footer clearfix">
    <div class="">
      <%= link_to new_grid_path do %>
        <i class="fa fa-plus" aria-hidden="true"></i>
        <%= t '.new', :default => t("helpers.titles.newa", model: model.model_name.human.downcase ) %>
      <% end %>
    </div>
  </div>
</div>
