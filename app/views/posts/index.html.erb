<%- model = Post -%>
<div class="panel panel-default">
  <div class="panel-heading clearfix">
    <h3 class="panel-title pull-left">
      <%= t '.title', :default => model.model_name.human.pluralize.titleize %>
    </h3>
    <div class="pull-right">
      <%= link_to new_post_path do %>
        <i class="fa fa-plus fa-2" aria-hidden="true"></i>
        <%= t('.new', :default => t("helpers.titles.new", model: model)) %>
      <% end %>
    </div>
  </div>
  <div class="panel-body">
    <div class="table-responsive">
      <%= content_tag :table, id: 'posts_datatable', class: 'table table-striped table-bordered', role: :datatable, data: { url: posts_path(format: :json)} do %>
        <thead>
          <tr>
            <th>#</th>
            <th>Título</th>
            <th></th>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tbody></tbody>
      <% end %>
    </div>
  </div>
  <div class="panel-footer clearfix">
    <div class="pull-right">
      <%= link_to new_post_path do %>
        <i class="fa fa-plus fa-2" aria-hidden="true"></i>
        <%= t('.new', :default => t("helpers.titles.new", model: model)) %>
      <% end %>
    </div>
  </div>
</div>
