<%- model = Post -%>
<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">
      <%= t '.title', :default => model.model_name.human.pluralize.titleize %>
    </h3>
  </div>
  <div class="panel-body">
    <div class="table-responsive">
      <%= content_tag :table, class: 'table table-striped table-bordered', role: :datatable, data: { url: posts_path(format: :json)} do %>
        <thead>
          <tr>
            <th>Título</th>
            <th>Ações</th>
          </tr>
        </thead>
        <tbody></tbody>
      <% end %>
    </div>
  </div>
  <div class="panel-footer">
    <%= link_to new_post_path do %>
      <span class="glyphicon glyphicon-plus"></span>
      <%= t('.new', :default => t("helpers.titles.new", model: model)) %>
    <% end %>
  </div>
</div>
