<div class="page-header">
  <%= link_to calendar_excludes_path, class: 'btn btn-default' do %>
    <i class='fa fa-list fa-2'></i>
    <%= t('helpers.links.back') %>
  <% end %>
  <%= link_to edit_calendar_exclude_path(@calendar_exclude), class: 'btn btn-primary' do %>
    <i class='fa fa-pencil-square-o fa-2'></i>
    <%= t('helpers.links.edit') %>
  <% end %>
  <h1>Detalhes calendar_exclude</h1>
</div>

<dl class="dl-horizontal">
  <dt>Calendar:</dt>
  <dd><%= @calendar_exclude.calendar %></dd>

  <dt>Dt exclusao:</dt>
  <dd><%= @calendar_exclude.dt_exclusao %></dd>

</dl>
