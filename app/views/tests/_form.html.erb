<%= bootstrap_form_for(test, local:true, html: { class: "", role: "form" }) do |f| %>
  <%= error_messages_for(test) %>


      <%= f.text_field :title, id: :test_title %>
      <%= f.text_field :body, id: :test_body %>
      <%= f.check_box :active, id: :test_active %>
  
  <%= f.submit class: "btn btn-primary" %>
<% end %>
